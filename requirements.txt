accelerate
# bitsandbytes  # CUDA-only, not needed on Ascend NPU
datasets
# deepspeed==0.16.7  # Ascend: use pre-installed deepspeed (deepspeed-npu), do NOT reinstall from PyPI
einops
flask
# flash-attn==2.7.4.post1  # CUDA-only, not available on Ascend NPU
isort
jsonlines
loralib
math-verify==0.5.2
levenshtein
optimum
optree>=0.13.0
packaging
peft
# pynvml>=12.0.0  # CUDA-only nvidia management library, not needed on Ascend NPU
qwen_vl_utils
# ray[default]==2.43.0  # Ascend: use pre-installed ray, version may differ
tensorboard
torchmetrics
tqdm
transformers>=4.51.3
transformers_stream_generator
wandb
wheel
loguru
torch_npu==2.9.0
torchvision 
deepspeed
aiohttp_cors
opentelemetry-api
opentelemetry-sdk
opencensus